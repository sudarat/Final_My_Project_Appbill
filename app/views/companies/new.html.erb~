<% flash .each do |key, value| %>
  <%= content_tag(:div, value, :class => "flash #{key}") %> 
<% end %>

<h1>กรุณาใส่ข้อมูลบริษัทของคุณ</h1>

<%= render 'form' %>

<% link_to back, request.referer %>
